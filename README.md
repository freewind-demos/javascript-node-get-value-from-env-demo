JavaScript Node Get Value From ENV Demo
=======================================

注意在`package.json` -> `scripts` -> `demo`里设置了环境变量`ENV userName=Freewind`

```
npm install
npm run demo
```

It will print:

```
Hello, Freewind
```